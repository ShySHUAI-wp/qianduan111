以下为命令行中使用相关命令示例：
## leader机械臂

### 校准命令
```bash
lerobot-calibrate \
    --teleop.type=so101_leader \
    --teleop.port=/dev/ttyACM1 \
    --teleop.id=my_awesome_leader_arm
```
之后程序会寻找`/home/shuo/.cache/huggingface/lerobot/calibration/teleoperators/so101_leader/my_awesome_leader_arm.json` 即对应teleop.id的json

### 源代码执行流程

校准命令会依次执行以下代码（[lerobot_calibrate.py:85-87](src/lerobot/scripts/lerobot_calibrate.py#L85-L87)）：
```python
device.connect(calibrate=False)  # 连接设备但不自动校准
device.calibrate()               # 调用校准方法
device.disconnect()              # 断开连接
```

具体的校准逻辑在 [so101_leader.py:88-126](src/lerobot/teleoperators/so101_leader/so101_leader.py#L88-L126)：
```python
def calibrate(self) -> None:
    if self.calibration:
        # 如果已有校准文件，询问用户是否使用
        user_input = input(
            f"Press ENTER to use provided calibration file associated with the id {self.id}, "
            f"or type 'c' and press ENTER to run calibration: "
        )
        if user_input.strip().lower() != "c":
            logger.info(f"Writing calibration file associated with the id {self.id} to the motors")
            self.bus.write_calibration(self.calibration)
            return

    logger.info(f"\nRunning calibration of {self}")
    self.bus.disable_torque()  # 禁用扭矩，允许手动移动
    for motor in self.bus.motors:
        self.bus.write("Operating_Mode", motor, OperatingMode.POSITION.value)

    # 设置中间位置
    input(f"Move {self} to the middle of its range of motion and press ENTER....")
    homing_offsets = self.bus.set_half_turn_homings()

    # 记录运动范围
    print(
        "Move all joints sequentially through their entire ranges "
        "of motion.\nRecording positions. Press ENTER to stop..."
    )
    range_mins, range_maxes = self.bus.record_ranges_of_motion()

    # 构建并保存校准数据
    self.calibration = {}
    for motor, m in self.bus.motors.items():
        self.calibration[motor] = MotorCalibration(
            id=m.id,
            drive_mode=0,
            homing_offset=homing_offsets[motor],
            range_min=range_mins[motor],
            range_max=range_maxes[motor],
        )

    self.bus.write_calibration(self.calibration)
    self._save_calibration()
    print(f"Calibration saved to {self.calibration_fpath}")
```

### 存在校准文件和id匹配的情况：

输入后如果存在之前的校准信息会输出:
```bash
(lerobot) shuo@shuo-Legion-Y9000P-IRX8:~$ lerobot-calibrate \
    --teleop.type=so101_leader \
    --teleop.port=/dev/ttyACM1 \
    --teleop.id=my_awesome_leader_arm
INFO 2026-02-03 11:35:35 calibrate.py:78 {'robot': None,
 'teleop': {'calibration_dir': None,
            'id': 'my_awesome_leader_arm',
            'port': '/dev/ttyACM1',
            'use_degrees': False}}
INFO 2026-02-03 11:35:35 01_leader.py:82 my_awesome_leader_arm SO101Leader connected.
Press ENTER to use provided calibration file associated with the id my_awesome_leader_arm, or type 'c' and press ENTER to run calibration:
```

这说明
有对应`teleop.id`存在,可以直接使用ENTER使用此时输出
```bash
INFO 2026-02-03 11:48:04 01_leader.py:95 Writing calibration file associated with the id my_awesome_leader_arm to the motors
INFO 2026-02-03 11:48:04 1_leader.py:156 my_awesome_leader_arm SO101Leader disconnected.
```
直接可以配对 

或者此时输入c后回车可以直接重置校准，之后会进入校准阶段，终端会输出：
```bash
INFO 2026-02-03 11:37:00 01_leader.py:99 
Running calibration of my_awesome_leader_arm SO101Leader
Move my_awesome_leader_arm SO101Leader to the middle of its range of motion and press ENTER....
```

此时会终端输出`Move my_awesome_leader_arm SO101Leader to the middle of its range of motion and press ENTER....`
此时需要把机械臂手动移动到可活动角度的中间位置，当所有都为中间位置之后，点击回车

之后终端输出`Recording positions. Press ENTER to stop..`,此时需要手动将机械臂自行移动到其行程最大和最小角度，程序会不断记录最大值和最小值，完成后回车

之后输出
```bash
Calibration saved to /home/shuo/.cache/huggingface/lerobot/calibration/teleoperators/so101_leader/my_awesome_leader_arm.json
INFO 2026-02-03 11:44:30 1_leader.py:156 my_awesome_leader_arm SO101Leader disconnected.
```
说明对应配置的json文件已经被重写或覆盖，至此调试完毕

### 不存在校准文件和id匹配的情况：
此时终端会输出：
```bash
(lerobot) shuo@shuo-Legion-Y9000P-IRX8:~$ lerobot-calibrate     --teleop.type=so101_leader     --teleop.port=/dev/ttyACM1     --teleop.id=my_awesome_leader_arm
INFO 2026-02-03 11:51:33 calibrate.py:78 {'robot': None,
 'teleop': {'calibration_dir': None,
            'id': 'my_awesome_leader_arm',
            'port': '/dev/ttyACM1',
            'use_degrees': False}}
INFO 2026-02-03 11:51:33 01_leader.py:82 my_awesome_leader_arm SO101Leader connected.
INFO 2026-02-03 11:51:33 01_leader.py:99 
Running calibration of my_awesome_leader_arm SO101Leader
Move my_awesome_leader_arm SO101Leader to the middle of its range of motion and press ENTER....
```

此时说明系统未找到`/home/shuo/.cache/huggingface/lerobot/calibration/teleoperators/so101_leader/`路径向同`teleop.id`的校准文件，此时已经直接进入了校准阶段，人工手动把设备移动到电机或者舵机中间位置后点击回车,此时终端会输出:

```bash
Recording positions. Press ENTER to stop..
```
此时需要手动将机械臂自行移动到其行程最大和最小角度，程序会不断记录最大值和最小值，完成后回车,终端输出

```bash
Calibration saved to /home/shuo/.cache/huggingface/lerobot/calibration/teleoperators/so101_leader/my_awesome_leader_arm.json
INFO 2026-02-03 11:56:06 1_leader.py:156 my_awesome_leader_arm SO101Leader disconnected.
```
说明对应配置的json文件已经被创建重写或覆盖，至此调试完毕

### 常见失误操作及错误提示

#### 1. 设置完中间值后未移动机械臂到最大/最小角度

**错误场景**：在提示`Recording positions. Press ENTER to stop..`时，用户没有手动将机械臂移动到各个关节的最大和最小角度，而是直接按回车。

**错误提示**：
```bash
Recording positions. Press ENTER to stop..

-------------------------------------------
NAME            |    MIN |    POS |    MAX
shoulder_pan    |   2048 |   2048 |   2048
shoulder_lift   |   2048 |   2048 |   2048
elbow_flex      |   2048 |   2048 |   2048
wrist_flex      |   2048 |   2048 |   2048
wrist_roll      |   2048 |   2048 |   2048
gripper         |   2048 |   2048 |   2048

Traceback (most recent call last):
  ...
ValueError: Some motors have the same min and max values:
['shoulder_pan',
 'shoulder_lift',
 'elbow_flex',
 'wrist_flex',
 'wrist_roll',
 'gripper']
```

**错误原因**：程序通过 [motors_bus.py:764-766](src/lerobot/motors/motors_bus.py#L764-L766) 检测到某些电机的最小值和最大值相同：
```python
same_min_max = [motor for motor in motors if mins[motor] == maxes[motor]]
if same_min_max:
    raise ValueError(f"Some motors have the same min and max values:\n{pformat(same_min_max)}")
```

**解决方法**：重新运行校准命令，在`Recording positions. Press ENTER to stop..`提示时，**必须**手动将每个关节依次移动到其物理行程的最大角度和最小角度，确保程序记录到完整的运动范围。

#### 2. 部分关节未移动到极限位置

**错误场景**：用户只移动了部分关节到最大/最小角度，其他关节保持在中间位置或只做了微小移动。

**错误提示**：
```bash
ValueError: Some motors have the same min and max values:
['wrist_roll', 'gripper']
```

**解决方法**：重新校准，确保**所有关节**（shoulder_pan、shoulder_lift、elbow_flex、wrist_flex、wrist_roll、gripper）都完整移动到最大和最小角度。

#### 3. 未在中间位置按回车就移动机械臂

**错误场景**：在提示`Move my_awesome_leader_arm SO101Leader to the middle of its range of motion and press ENTER....`时，用户未等机械臂完全移动到中间位置就按了回车。

**后果**：虽然不会立即报错，但会导致后续的 `homing_offset` 设置不准确，最终校准结果不正确，可能在遥操作时出现角度偏移或行程受限。

**解决方法**：在按回车前，仔细检查所有关节是否都处于其可活动角度的中间位置（通常目视即可判断）。

## follower机械臂

### 校准命令
```bash
lerobot-calibrate \
    --robot.type=so101_follower \
    --robot.port=/dev/ttyACM0 \
    --robot.id=my_awesome_follower_arm
```
之后程序会寻找`/home/shuo/.cache/huggingface/lerobot/calibration/robots/so101_follower/my_awesome_follower_arm.json` 即对应robot.id的json

### 源代码执行流程

校准命令会依次执行以下代码（[lerobot_calibrate.py:80-87](src/lerobot/scripts/lerobot_calibrate.py#L80-L87)）：
```python
if isinstance(cfg.device, RobotConfig):
    device = make_robot_from_config(cfg.device)
elif isinstance(cfg.device, TeleoperatorConfig):
    device = make_teleoperator_from_config(cfg.device)

device.connect(calibrate=False)  # 连接设备但不自动校准
device.calibrate()               # 调用校准方法
device.disconnect()              # 断开连接
```

具体的校准逻辑在 [so101_follower.py:110-147](src/lerobot/robots/so101_follower/so101_follower.py#L110-L147)：
```python
def calibrate(self) -> None:
    if self.calibration:
        # 如果已有校准文件，询问用户是否使用
        user_input = input(
            f"Press ENTER to use provided calibration file associated with the id {self.id}, "
            f"or type 'c' and press ENTER to run calibration: "
        )
        if user_input.strip().lower() != "c":
            logger.info(f"Writing calibration file associated with the id {self.id} to the motors")
            self.bus.write_calibration(self.calibration)
            return

    logger.info(f"\nRunning calibration of {self}")
    self.bus.disable_torque()  # 禁用扭矩，允许手动移动
    for motor in self.bus.motors:
        self.bus.write("Operating_Mode", motor, OperatingMode.POSITION.value)

    # 设置中间位置
    input(f"Move {self} to the middle of its range of motion and press ENTER....")
    homing_offsets = self.bus.set_half_turn_homings()

    # 记录运动范围
    print(
        "Move all joints sequentially through their entire ranges "
        "of motion.\nRecording positions. Press ENTER to stop..."
    )
    range_mins, range_maxes = self.bus.record_ranges_of_motion()

    # 构建并保存校准数据
    self.calibration = {}
    for motor, m in self.bus.motors.items():
        self.calibration[motor] = MotorCalibration(
            id=m.id,
            drive_mode=0,
            homing_offset=homing_offsets[motor],
            range_min=range_mins[motor],
            range_max=range_maxes[motor],
        )

    self.bus.write_calibration(self.calibration)
    self._save_calibration()
    print("Calibration saved to", self.calibration_fpath)
```

### 存在校准文件和id匹配的情况

输入后如果存在之前的校准信息会输出:
```bash
(lerobot) shuo@shuo-Legion-Y9000P-IRX8:~$ lerobot-calibrate \
    --robot.type=so101_follower \
    --robot.port=/dev/ttyACM0 \
    --robot.id=my_awesome_follower_arm
INFO 2026-02-03 11:35:35 calibrate.py:78 {'robot': {'calibration_dir': None,
            'id': 'my_awesome_follower_arm',
            'port': '/dev/ttyACM0',
            'use_degrees': False},
 'teleop': None}
INFO 2026-02-03 11:35:35 so101_follower.py:104 my_awesome_follower_arm SO101Follower connected.
Press ENTER to use provided calibration file associated with the id my_awesome_follower_arm, or type 'c' and press ENTER to run calibration:
```

这说明有对应`robot.id`存在,可以直接使用ENTER使用此时输出
```bash
INFO 2026-02-03 11:48:04 so101_follower.py:117 Writing calibration file associated with the id my_awesome_follower_arm to the motors
INFO 2026-02-03 11:48:04 so101_follower.py:230 my_awesome_follower_arm SO101Follower disconnected.
```
直接可以配对

或者此时输入c后回车可以直接重置校准，之后会进入校准阶段，终端会输出：
```bash
INFO 2026-02-03 11:37:00 so101_follower.py:121
Running calibration of my_awesome_follower_arm SO101Follower
Move my_awesome_follower_arm SO101Follower to the middle of its range of motion and press ENTER....
```

此时会终端输出`Move my_awesome_follower_arm SO101Follower to the middle of its range of motion and press ENTER....`
此时需要把机械臂手动移动到可活动角度的中间位置，当所有都为中间位置之后，点击回车

之后终端输出`Recording positions. Press ENTER to stop..`,此时需要手动将机械臂自行移动到其行程最大和最小角度，程序会不断记录最大值和最小值，完成后回车

之后输出
```bash
Calibration saved to /home/shuo/.cache/huggingface/lerobot/calibration/robots/so101_follower/my_awesome_follower_arm.json
INFO 2026-02-03 11:44:30 so101_follower.py:230 my_awesome_follower_arm SO101Follower disconnected.
```
说明对应配置的json文件已经被重写或覆盖，至此调试完毕

### 不存在校准文件和id匹配的情况
此时终端会输出：
```bash
(lerobot) shuo@shuo-Legion-Y9000P-IRX8:~$ lerobot-calibrate \
    --robot.type=so101_follower \
    --robot.port=/dev/ttyACM0 \
    --robot.id=my_awesome_follower_arm
INFO 2026-02-03 11:51:33 calibrate.py:78 {'robot': {'calibration_dir': None,
            'id': 'my_awesome_follower_arm',
            'port': '/dev/ttyACM0',
            'use_degrees': False},
 'teleop': None}
INFO 2026-02-03 11:51:33 so101_follower.py:104 my_awesome_follower_arm SO101Follower connected.
INFO 2026-02-03 11:51:33 so101_follower.py:121
Running calibration of my_awesome_follower_arm SO101Follower
Move my_awesome_follower_arm SO101Follower to the middle of its range of motion and press ENTER....
```

此时说明系统未找到`/home/shuo/.cache/huggingface/lerobot/calibration/robots/so101_follower/`路径下同`robot.id`的校准文件，此时已经直接进入了校准阶段，人工手动把设备移动到电机或者舵机中间位置后点击回车,此时终端会输出:

```bash
Recording positions. Press ENTER to stop..
```
此时需要手动将机械臂自行移动到其行程最大和最小角度，程序会不断记录最大值和最小值，完成后回车,终端输出

```bash
Calibration saved to /home/shuo/.cache/huggingface/lerobot/calibration/robots/so101_follower/my_awesome_follower_arm.json
INFO 2026-02-03 11:56:06 so101_follower.py:230 my_awesome_follower_arm SO101Follower disconnected.
```
说明对应配置的json文件已经被创建重写或覆盖，至此调试完毕

### 常见失误操作及错误提示

#### 1. 设置完中间值后未移动机械臂到最大/最小角度

**错误场景**：在提示`Recording positions. Press ENTER to stop..`时，用户没有手动将机械臂移动到各个关节的最大和最小角度，而是直接按回车。

**错误提示**：
```bash
Recording positions. Press ENTER to stop..

-------------------------------------------
NAME            |    MIN |    POS |    MAX
shoulder_pan    |   2048 |   2048 |   2048
shoulder_lift   |   2048 |   2048 |   2048
elbow_flex      |   2048 |   2048 |   2048
wrist_flex      |   2048 |   2048 |   2048
wrist_roll      |   2048 |   2048 |   2048
gripper         |   2048 |   2048 |   2048

Traceback (most recent call last):
  ...
ValueError: Some motors have the same min and max values:
['shoulder_pan',
 'shoulder_lift',
 'elbow_flex',
 'wrist_flex',
 'wrist_roll',
 'gripper']
```

**错误原因**：程序通过 [motors_bus.py:764-766](src/lerobot/motors/motors_bus.py#L764-L766) 检测到某些电机的最小值和最大值相同：
```python
same_min_max = [motor for motor in motors if mins[motor] == maxes[motor]]
if same_min_max:
    raise ValueError(f"Some motors have the same min and max values:\n{pformat(same_min_max)}")
```

**解决方法**：重新运行校准命令，在`Recording positions. Press ENTER to stop..`提示时，**必须**手动将每个关节依次移动到其物理行程的最大角度和最小角度，确保程序记录到完整的运动范围。

#### 2. 部分关节未移动到极限位置

**错误场景**：用户只移动了部分关节到最大/最小角度，其他关节保持在中间位置或只做了微小移动。

**错误提示**：
```bash
ValueError: Some motors have the same min and max values:
['wrist_roll', 'gripper']
```

**解决方法**：重新校准，确保**所有关节**（shoulder_pan、shoulder_lift、elbow_flex、wrist_flex、wrist_roll、gripper）都完整移动到最大和最小角度。

#### 3. 未在中间位置按回车就移动机械臂

**错误场景**：在提示`Move my_awesome_follower_arm SO101Follower to the middle of its range of motion and press ENTER....`时，用户未等机械臂完全移动到中间位置就按了回车。

**后果**：虽然不会立即报错，但会导致后续的 `homing_offset` 设置不准确，最终校准结果不正确，可能在后续数据采集或控制时出现角度偏移或行程受限。

**解决方法**：在按回车前，仔细检查所有关节是否都处于其可活动角度的中间位置（通常目视即可判断）。

#### 4. follower机械臂特别注意事项

**⚠️ 重要**：与leader机械臂不同，follower机械臂在校准过程中需要**禁用扭矩**（通过 [so101_follower.py:122](src/lerobot/robots/so101_follower/so101_follower.py#L122) 的 `self.bus.disable_torque()` 实现）。这意味着：

- 校准时机械臂**不会提供任何阻力**，需要小心移动避免突然下坠
- 建议在桌面上进行校准，或者有人辅助支撑机械臂
- 移动时要缓慢、平稳，避免碰撞或超出物理限位

## 其他机械臂类型

本文档主要以 `so101_leader` 和 `so101_follower` 为例，但相同的校准流程也适用于其他机械臂类型：

### Leader机械臂类型
- `so100_leader`: SO-100 Leader Arm
- `bi_so100_leader`: 双臂 SO-100 Leader
- `so101_leader`: SO-101 Leader Arm（本文档示例）

### Follower机械臂类型
- `so100_follower`: SO-100 Follower Arm
- `bi_so100_follower`: 双臂 SO-100 Follower
- `so101_follower`: SO-101 Follower Arm（本文档示例）

**使用方法**：将上述命令中的 `--teleop.type` 或 `--robot.type` 参数替换为对应的机械臂类型即可，其他步骤完全相同